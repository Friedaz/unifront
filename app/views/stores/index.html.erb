<p id="notice"><%= notice %></p>


<h3>My Store</h3>
          <% current_user.store.each do |store| %>
        <div class="col-md-5">
              <div class="card mb-4 shadow-sm">
                <img class="card-img-top"<%= image_tag store.logo.url(:medium) %><alt="Card image cap">
                <div class="card-body">
                  <p class="card-text"><h3><%= store.name %></h3></p>
                  <b>Description : </b><p class="card-text"><%= store.description %></p>
                  <div class="d-flex justify-content-between align-items-center">
                    <div class="btn-group">
                      <button type="button" class="btn btn-sm btn-outline-secondary" ><%= link_to 'View', store %></button>
                      <button type="button" class="btn btn-sm btn-outline-secondary" ><%= link_to 'Edit', edit_store_path(store) %></button>
                      <button type="button" class="btn btn-sm btn-outline-secondary" ><%= link_to 'Delete', store, method: :delete, data: { confirm: 'Are you sure?' } %></button>
                    </div>
                  </div>
                </div>
              </div>
            </div>
            <% end %>
          </div>
      <hr>
<!-- <h1>Stores</h1>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Description</th>
      <th>Logo</th>
      <th>User</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @stores.each do |store| %>
      <tr>
        <td><%= store.name %></td>
        <td><%= store.description %></td>
        <td><%= store.logo %></td>
        <td><%= store.user %></td>
        <td><%= link_to 'Show', store %></td>
        <td><%= link_to 'Edit', edit_store_path(store) %></td>
        <td><%= link_to 'Destroy', store, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br> -->

<div class="btn-group">
    <button type="button" class="btn btn-sm btn-outline-secondary" ><%= link_to 'Create New Store', new_store_path %></button>
</div>


